# ODEO AI Studio 环境变量配置示例
# 复制此文件为 .env 并根据实际环境修改

# ============================================
# 存储路径配置
# ============================================

# 数据存储根目录
STORAGE_ROOT=./data

# 预训练模型目录
MODELS_ROOT=./pretrained_models

# 训练输出目录
TRAINING_OUTPUT_ROOT=./data/outputs

# 数据集目录
DATASET_PATH=./data/datasets
RAW_PATH=./data/datasets/raw

# 图库目录
GALLERY_ROOT=./data/gallery

# 推理输出目录
INFERENCE_OUTPUT_ROOT=./data/outputs/inference

# LoRA 模型目录
LORA_ROOT=./data/outputs

# ============================================
# API 配置
# ============================================

# Flask 密钥 (生产环境必须修改)
SECRET_KEY=your-secret-key-change-this

# 日志级别 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# CORS 允许的域名 (多个用逗号分隔)
CORS_ORIGINS=*

# ============================================
# ComfyUI 配置 (用于视频推理)
# ============================================

# ComfyUI 安装根目录
# COMFYUI_ROOT=/path/to/comfyui

# ComfyUI 各子目录（通常不需要单独设置，会从 COMFYUI_ROOT 自动派生）
# COMFYUI_INPUT_DIR=/path/to/comfyui/input
# COMFYUI_OUTPUT_DIR=/path/to/comfyui/output
# COMFYUI_LORAS_DIR=/path/to/comfyui/models/loras

# Workflow 文件路径（默认使用仓库内 configs/workflows/ 下的文件）
# COMFYUI_WORKFLOW_PATH=/path/to/workflow.json

# ============================================
# Conda 环境配置
# ============================================

# conda 环境名称（默认: lora）
# CONDA_ENV_NAME=lora

# ============================================
# 可选: Qwen API (用于视频描述生成和提示词优化)
# ============================================
# QWEN_VL_API_KEY=sk-your-api-key
